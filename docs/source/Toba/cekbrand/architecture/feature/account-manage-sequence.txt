actor User

participant Frontend
participant Backend

database Database

entryspacing 0.5

autoactivation 

autonumber 

activate User

User->Frontend:Open //Kelola Akun// page

autonumber off
Frontend->Frontend:Get user Facebook accounts\ndata, including their related\nInstagram account
autonumber 
Frontend->Backend:Fetch user Facebook accounts list
Backend->Database:Select Facebook\naccounts
autonumber off
Backend<<--Database:
autonumber 
Frontend<--Backend:Facebook accounts list
parallel
autonumber off
Frontend<--Frontend:
autoactivation off
autonumber 
User<<--Frontend:Display account manage page
parallel off

autoactivation 
autonumber off
Frontend->Frontend:Get user Instagram accounts\nand subscription groups
autonumber 
Frontend->Backend:Fetch user Instagram\naccounts list
Backend->Database:Selecr Instagram\naccounts
autonumber off
Backend<<--Database:
autonumber 
Frontend<--Backend:Instagram accounts list

Frontend->Backend:Fetch subscription\ngroups list
Backend->Database:Select subscription\ngroups
autonumber off
Backend<<--Database:
autonumber 
Frontend<--Backend:Subscription groups list
parallel
autonumber off
Frontend<--Frontend:
autoactivation off
autonumber 
User<<--Frontend:Display account manage limit
parallel off

deactivateafter User